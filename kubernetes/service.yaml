apiVersion: v1
kind: Service
metadata:
  labels:
    app: petclinic
  name: petclinic
  namespace: dev
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8082
  selector:
    app: petclinic 
  type: NodePort